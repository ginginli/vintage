{
  "name": "stock-analyzer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev --listen 4001",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "dotenv": "^10.0.0"
  }
}
